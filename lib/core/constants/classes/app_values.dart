import 'package:flutter/material.dart';

/// Default padding for the pages
const kPagePadding = EdgeInsets.symmetric(
  horizontal: 20,
  vertical: 40,
);

/// Padding for the pages without horizontal padding
const kPagePaddingNoHorizontal = EdgeInsets.symmetric(
  vertical: 40,
);
